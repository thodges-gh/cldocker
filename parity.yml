version: '3.1'
services:
  geth:
    hostname: eth
    image: parity/parity:stable
    tty: true
    volumes:
      - "./ethereum:/ethereum"
    ports:
    - "8546:8546"
    - "30303:30303"
    command: ["--config", "/ethereum/parity.toml"]
    restart: always
volumes:
  ethereum: